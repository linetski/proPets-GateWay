zuul:
 routes:
  lostAndFoundMicroService: 
    path: /lostAndFound/**
    serviceId: lostAndFoundMicroService
  propets-authentification-service: 
    path: /api/auth/**
    serviceId: propets-authentification-service




eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://user:password@localhost:5000}/eureka/
#security:
# user:
#    password: ${eureka.password}

#---
#spring:
#  profiles: heroku
#eureka:
#  instance:
#    hostname: ${DOMAIN_NAME}
#    nonSecurePort: 80
#  password: ${USER_PASSWORD:password}