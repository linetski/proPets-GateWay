zuul:
 routes:
  lostAndFoundService: 
    path: /lostAndFound/**
    serviceId: lostAndFoundService
  propets-authentification-service: 
    path: /api/auth/**
    serviceId: propets-authentification-service




eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:https://propets-eurekaservice.herokuapp.com}/eureka/
#security:
# user:
#    password: ${eureka.password}

#---
#spring:
#  profiles: heroku
#eureka:
#  instance:
#    hostname: ${DOMAIN_NAME}
#    nonSecurePort: 80
#  password: ${USER_PASSWORD:password}